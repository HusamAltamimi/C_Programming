#include <stdio.h>

int main () {
    int x = 12;
    int result1 = x % 8; // Will show the Remainder
    printf("%d", result1); 
    /*
        double y = 12;
        double result2 = y % 8; // Error! Remainder only work with integers
        printf("\n%f", result2); 

        float z = 12;
        float result3 = z % 8; // Error! Remainder only work with integers
        printf("\n%f", result2); 
        return 0;
    */
}