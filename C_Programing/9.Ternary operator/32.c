#include <stdio.h>
/*
creat a ternary program to check if the number is odd or even?
*/
int main (){
    int a = 50;
    (a % 2 == 0) ? printf("The number is Even") : printf("The number is Odd");

    int result;
    result = 5 > 9 ? 5 : 9;
    printf("\nResult : %d", result);

    return 0;
}