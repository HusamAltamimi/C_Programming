#include <stdio.h>

int main() {
    
    int age[5] = {25 , 29 , 31 , 17 , 21}; // Assign array type / name / size / elements
    printf("%d", age[0]); // Accessing array element by indexing

    return 0;
}
